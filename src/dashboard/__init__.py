"""src/dashboard — Phase 7: Streamlit analytics dashboard.

Entry point:
    streamlit run src/dashboard/app.py

Package structure:
    app.py          — Main entry: navigation, page config, global CSS
    api_client.py   — All HTTP requests (pages never import requests directly)
    pages/          — One render() function per page
    components/     — Reusable UI building blocks (metrics, charts, tables)
"""
